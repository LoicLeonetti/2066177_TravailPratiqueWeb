{% extends('baseAdmin.html.twig') %}

{% block body %}

	<!-- Nom: Loic Leonetti -->
	<!-- Date: 2024-05-22   -->
		<div>
	{{ include('commun/ZoneFlash.html.twig') }}
	</div>
		<div class='container row my-3'> <div class="col-7">

			<h2>Modifier l'image de couverture du produit {{produit.nom}}({{produit.id}})</h2>


			{{ form_start(form) }}
			{{ form_label(form.image_produit, "Photo chômeur (max 50 Ko):") }}
			{{ form_errors(form.image_produit) }}
			<input type='Hidden' value='50000' name='MAX_FILE_SIZE'>
			{{ form_widget(form.image_produit) }}
			{{ form_rest(form) }}
			{{ form_end(form) }}


		</div>
	{% endblock %}
