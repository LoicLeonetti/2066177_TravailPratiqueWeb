
{% extends('baseAdmin.html.twig') %}

{% block body %}

	<!-- Nom: Loic Leonetti -->
	<!-- Date: 2024-05-16   -->
	<div>
	{{ include('commun/ZoneFlash.html.twig') }}
	</div>
	<div class='container row my-3'>

		<div class="col-7">
			<h2>Produits à commander</h2>

            <table class="table table-striped table-bordered">
                <tr>
                    <th>Nom</th>
                    <th>Prix</th>
                    <th>En stock</th>
                    <th>Minimale</th>
                </tr>
                
                {% for p in produits %}
                <tr>
                    <td>{{ p.nom }}</td>
                    <td>{{ p.prix}}</td>
                    <td>{{ p.getQtteStock() }}</td>
                    <td>{{ p.getQtteSeuilMin() }}</td>
                {% endfor %}

            </table>

		</div>
		<div class="col-4">
			<a href="{{path('adminMenu')}}"><button class="btn btn-warning">Retour au menu</button></a>
		</div>
		


	</div>
{% endblock %}

